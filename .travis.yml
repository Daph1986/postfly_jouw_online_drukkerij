language: python
python:
  - "3.8.2"

# command to install dependencies
install:
  - pip install -r requirements.txt
script:
  - python manage.py test --noinput --settings postfly_jouw_online_drukkerij.settings_test